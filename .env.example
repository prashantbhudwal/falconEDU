# OpenAI API key used for natural language processing
OPENAI_API_KEY=abc123def456

# Mixpanel token used for analytics tracking
DEV_MIXPANEL_TOKEN=xyz789uvw123

# URL for accessing a key-value store
KV_URL=https://example.com/kv

# URL for accessing a REST API for the key-value store
KV_REST_API_URL=https://example.com/api/kv

# Token used for authentication when accessing the key-value store via the REST API
KV_REST_API_TOKEN=abc123def456

# Token used for read-only access when accessing the key-value store via the REST API
KV_REST_API_READ_ONLY_TOKEN=xyz789uvw123

# URL for accessing a development server directly
DIRECT_URL_DEV=https://dev.example.com

# URL for accessing a development database
DATABASE_URL_DEV=https://dev.example.com/db

# Google OAuth2 client ID
GOOGLE_CLIENT_ID=abc123def456

# Google OAuth2 client secret
GOOGLE_CLIENT_SECRET=xyz789uvw123

# Secret used for Next.js authentication
NEXTAUTH_SECRET=abc123def456

# URL used for Next.js authentication
NEXTAUTH_URL=https://example.com/auth

# API endpoint used by the Next.js app
NEXT_PUBLIC_API_ENDPOINT=https://api.example.com

# Stripe publishable key used for payment processing
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=abc123def456

# Stripe secret key used for payment processing
STRIPE_SECRET_KEY=xyz789uvw123

# Secret used for verifying Stripe webhooks
STRIPE_WEBHOOK_SECRET=abc123def456
